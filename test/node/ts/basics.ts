import { test } from "uvu";
import * as assert from "uvu/assert";
import spotprice from "../../..";

// String spotprice
test("ToDo", function () {
    let result = true;
    assert.equal(result, true);
});

test.run();